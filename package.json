{
  "name": "ai-marketing-system",
  "version": "1.0.0",
  "private": true,
  "description": "AI Marketing System - A multi-module business platform",
  "scripts": {
    "justright:dev": "npm run dev:all --prefix modules/justright-calendar",
    "justright:build": "npm run build --prefix modules/justright-calendar",
    "install:all": "npm install && npm install --prefix modules/justright-calendar && npm install --prefix modules/justright-calendar/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
